services:
  gorse:
    cpu_shares: 512
    deploy:
      resources:
        limits:
          cpus: "{{gorse_cpu_limit}}"
          memory: "{{gorse_mem_limit}}"
        reservations:
          memory: "{{gorse_mem_res}}"
