name: searcher
title: Full-text indexing and search, served by OpenSearch.
optional: true

requires: []

ops:
  services:
    - opensearch
    - opensearch-setup
  profile: search
  gateway: []
  env:
    - OPENSEARCH_URL

sql: null
protocol: protocol/searcher.proto
export: ./features

routes: []
