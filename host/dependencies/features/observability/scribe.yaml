name: observability
title: Shipping the logs to an external collector.
optional: true

requires: []

ops:
  services:
    - vector
    - analytics
  profile: ops
  gateway: []
  env:
    - LOGFLARE_PUBLIC_ACCESS_TOKEN
    - LOGFLARE_PRIVATE_ACCESS_TOKEN

sql: null
protocol: protocol/observability.proto
export: ./features

routes: []
