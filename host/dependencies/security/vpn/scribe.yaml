name: vpn
title: Administrator VPN access, served by wg-easy.
optional: true

requires:
  - security/auth

ops:
  services:
    - vpn-admins
  profile: ops
  gateway: []
  env:
    - VPN_DOMAIN

sql: db/init
protocol: protocol/vpn.proto
export: ./security/vpn

routes: []
