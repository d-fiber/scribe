services:
  auth:
    cpu_shares: 2048
    deploy:
      resources:
        limits:
          memory: "{{auth_mem_limit}}"
        reservations:
          memory: "{{auth_mem_res}}"
  db-dev-confirm:
    deploy:
      resources:
        limits:
          memory: "{{db_dev_confirm_mem_limit}}"
